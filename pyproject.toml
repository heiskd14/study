[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "flask-session>=0.8",
    "flask-socketio",
    "bcrypt",
    "openai",
    "werkzeug",
    "pymongo",
    "requests>=2.32",
    "gunicorn",
    "eventlet",
]
